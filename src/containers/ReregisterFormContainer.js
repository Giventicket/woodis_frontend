import React from "react";
import { ReregisterForm } from "../components";

const ReregisterFormContainer = () => {
  return <ReregisterForm />;
};

export default React.memo(ReregisterFormContainer);
